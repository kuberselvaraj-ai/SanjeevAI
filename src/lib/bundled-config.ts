/**
 * Optional: bake a default Kimi API key into a LOCAL build so the app works
 * out of the box on your own machines.
 *
 * SECURITY: never commit a real key here — the GitHub repo is public.
 * The key is injected locally right before building the installer and the
 * file is restored afterwards.
 */
export const BUNDLED_MOONSHOT_KEY = ''
